import './stores';
import './styles/theme.css';

/**
 * Shared State MFE Bootstrap
 *
 * Initializes global state stores and styles.
 *
 * ```mermaid
 * graph LR
 *   A[bootstrap.tsx] --> B[Initialize Stores]
 *   A --> C[Load Styles]
 *   B --> D[Shared State Ready]
 * ```
 */

