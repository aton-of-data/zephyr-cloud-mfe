/**
 * ThemeButton Module
 *
 * Theme button atom component for the Remote application.
 * Barrel export for the ThemeButton component.
 *
 * ```mermaid
 * graph LR
 *   A[ThemeButton Module] --> B[ThemeButton Component]
 *   B --> C[Theme Store]
 *   C --> D[Toggle Theme Action]
 * ```
 *
 * @module remote/src/components/atoms/ThemeButton
 * @category Remote
 */
export { default } from './ThemeButton';

