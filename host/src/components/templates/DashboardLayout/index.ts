/**
 * DashboardLayout Template
 *
 * Main shell layout for the Host application.
 * Barrel export for the DashboardLayout template component.
 *
 * ```mermaid
 * graph LR
 *   A[DashboardLayout Module] --> B[DashboardLayout Component]
 *   B --> C[Header + Sidebar]
 *   B --> D[Remote Content Area]
 * ```
 *
 * @module host/src/components/templates/DashboardLayout
 * @category Host
 */
export { default } from './DashboardLayout';

