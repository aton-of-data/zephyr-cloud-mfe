/**
 * Header Module
 *
 * Header component for the Host application.
 * Barrel export for the Header organism component.
 *
 * ```mermaid
 * graph LR
 *   A[Header Module] --> B[Header Component]
 *   B --> C[Menu Toggle]
 *   B --> D[Title Display]
 * ```
 *
 * @module host/src/components/organisms/Header
 * @category Host
 */
export { default } from './Header';

